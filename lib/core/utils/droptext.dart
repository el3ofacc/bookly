import 'package:flutter/material.dart';

class Droptext extends StatelessWidget {
  const Droptext({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('choose book',style: TextStyle(fontSize: 20,color: Colors.white),);
  }
}